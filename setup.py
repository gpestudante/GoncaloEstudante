import os

from setuptools import setup, find_packages
setup(
    name="GoncaloEstudante",
    version="0.1",
    description="GoncaloEstudante Distribution Package",
    packages=find_packages(),
    include_package_data=True
)